@model IEnumerable<CRVS.Core.Models.BirthCertificate>
@{
    ViewData["Title"] = "To Edit Stage";
}

<h1 class="text-center mt-5">قائمة الشهادات الغير مكتملة للمراجعة</h1>

@*<table class="table table-dark table-responsive-lg mb-4" id="myTable">*@
<table class="table table-bordered table-responsive-lg mt-3">
    <thead>
        <tr>
            <th>Child Name</th>
            <th>Gender</th>
            <th>Governorate</th>
            <th>Directorate</th>
            <th>District</th>
            <th>Nahia</th>
            <th>Village</th>
            <th>الحالة</th>
            <th>عمليات</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var certificate in Model)
        {
            <tr>
                <td>@certificate.ChildName</td>
                <td>@certificate.Gender</td>
                <td>@certificate.Governorate</td>
                <td>@certificate.Doh</td>
                <td>@certificate.District</td>
                <td>@certificate.Nahia</td>
                <td>@certificate.Village</td>
                <td class="position-relative">
                    @{
                        var status = certificate.BiostatisticsStage ? "bg-success" : "bg-warning";
                        var title = certificate.BiostatisticsStage ? "مكتملة" : "غير مكتملة";
                    }
                    <div class="rounded-circle @status position-absolute" style="width: 10px; height: 10px; top: 50%; transform: translate(-50%, -50%); left: 50%;" title="@title"></div>
                </td>
                <td class="dropdown">
                    <a class="nav-link text-center m-0" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-three-dots fs-4"></i>
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <li><a asp-action="Edit" class="dropdown-item ps-1" asp-route-id="@certificate.BirthCertificateId"><i class="bi bi-pencil-square ms-3"></i> تعديل</a></li>
                        <li><a asp-action="Delete" class="dropdown-item ps-1" asp-route-id="@certificate.BirthCertificateId"><i class="bi bi-trash3 ms-3"></i> حذف</a></li>
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>
    @*    <li><a asp-action="Print" class="dropdown-item ps-1" asp-route-id="@item.BirthCertificateId"><i class="bi bi-printer ms-3"></i> Print</a></li>
        <li><a asp-action="Details" class="dropdown-item ps-1" asp-route-id="@item.BirthCertificateId"><i class="bi bi-info-circle ms-3"></i> Details</a></li>
    *@